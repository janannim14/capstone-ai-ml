# CardioRisk Production Core - Environment Variables Template
# (Note: Current production build uses standard rule-based parsing matrices)

APP_SECURITY_ENVIRONMENT=production
GUARDRAIL_STRICTNESS_LEVEL=high